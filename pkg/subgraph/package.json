{
  "name": "subgraph",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "graph codegen && graph build",
    "create-local": "graph create --node http://localhost:8020/ blossom/osmotic-fund",
    "remove-local": "graph remove --node http://localhost:8020/ blossom/osmotic-fund",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 blossom/osmotic-fund",
    "manifest-local": "scripts/build-manifest.sh local",
    "manifest-goerli": "scripts/build-manifest.sh goerli",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.46.0",
    "@graphprotocol/graph-ts": "^0.29.1"
  },
  "devDependencies": {
    "assemblyscript": "0.19.20",
    "matchstick-as": "^0.5.1",
    "mustache": "^4.2.0"
  }
}
