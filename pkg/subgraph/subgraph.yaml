# Do not edit subgraph.yaml, this is a generated file.
# Instead, edit subgraph.template.yaml and run `yarn manifest-<network>-[staging]`
specVersion: 0.0.4
description: A template for the Osmotic Fund subgraph
repository: https://github.com/BlossomLabs/OsmoticFund
schema:
  file: ./schema.graphql
dataSources:
# Mime token factories
  # MimeTokenFactory
  - kind: ethereum/contract
    name: MimeTokenFactory@0.0.1
    network: goerli
    source:
      abi: MimeTokenFactory
      address: '0x357938548a20B910ae1F59Dc09CE37eA48856254'
      startBlock: 8552411
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      entities:
        - OsmoticController
        - OsmoticPool
        - Project
        - ProjectList
        - ProjectRegistry
      abis:
        - name: MimeTokenFactory
          file: ./abis/MimeTokenFactory.json
        - name: MimeToken
          file: ./abis/MimeToken.json
      eventHandlers:
        - event: MimeTokenCreated(address)
          handler: handleMimeTokenCreated
      file: ./src/mappings/MimeTokenFactory.ts
# /Mime token factories
